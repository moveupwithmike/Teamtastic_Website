# TEAMTASTIC LEGAL / TRUST / CUSTOMER-POLICY LAUNCH CLOSURE

Date: August 29, 2026 · Scope: one repo (`teamtastic.events` Next.js app) · Method: source inspection + automated tests + production-build browser verification. No external counsel; every statement below is grounded in repo evidence or explicitly flagged as owner-input.

## 1. Audit scope, constraints, and verification method

- Inventoried every customer-facing and office surface that touches legal/trust/payments/cancellation: footer, lead forms, booking, Checkout, Stripe webhooks, office refund actions, email delivery, analytics/consent, pricing, sitemap/robots, blog/FAQ/resources.
- Hard constraints honored: no invented legal claims (no GDPR/CCPA/HIPAA/SOC2/PCI assertions), no fabricated entity/address/jurisdiction, no raw placeholder tokens left user-visible; safe temporary wording used where an owner decision is pending and flagged in §11/§27.
- Verification: `npm test` (298 passing), `npm run lint`, `npm run typecheck`, `npm run build` (policy routes statically prerendered), plus Playwright browser QC (16/16) against the production build on `:3111` and final repo-wide consistency re-searches (§24–26).

## 2. Working tree state (final)

- Everything gate-clean. One non-source artifact removed during the run: a stale `.next/types` directory that produced spurious duplicate-identifier type errors (regenerated by the build; no source impact).
- New files: `src/components/PolicyShell.js`, `src/app/privacy/page.js`, `src/app/terms/page.js`, `src/app/cancellation-policy/page.js`, `src/app/policy-pages.test.js`.
- Edited files: `src/components/Footer.js`, `src/components/SoloDemo.js`, `src/components/CorporateLeadForm.js`, `src/components/TalkToMichaelModal.js`, `src/components/GameQuiz.js`, `src/components/HolidayChecklistForm.js`, `src/lib/cancellation-policy.js`, `src/lib/cancellation-policy.test.js`, `src/app/resources/faq/page.js`, `src/app/sitemap.js`, `src/app/sitemap.test.js`.

## 3. Pre-existing policy/trust surface inventory and gap analysis

- Before this workstream: no `/privacy`, `/terms`, or `/cancellation-policy` routes existed (`src/app` glob confirmed). Footer Privacy/Terms were plain `<span>`s; the experiences footer had four dead `href="#"` social icons and a hardcoded `© 2024`. Lead forms stated "No obligation" but carried no privacy/terms consent or deposit disclosure.
- Existing (usable) truth: `src/lib/cancellation-policy.js` tier engine, FAQ refund answer mirroring those tiers, product pages already disclosing the $200/$100 deposits, `robots.js` allow-all with sitemap, and a live consent banner (`src/components/ConsentBanner.js`).

## 4. Delivered policy pages (+ PolicyShell)

- `PolicyShell.js`: shared dark zinc-950 shell with `Last updated:` marker, used by all three pages for visual consistency.
- `/privacy`: collection/use, cookies & analytics, processor list (Supabase, Stripe, Resend, Zoom, Google Calendar, Cloudflare Turnstile, PostHog, Meta, Google, and Calendly where configured), UGC, children, retention (no auto-deletion — accurate to repo), rights, cross-border transfers, security, changes, contact.
- `/terms`: acceptance, services, quotes/pricing/payment (deposits $200/$100, balance invoiced, PO support), cancellation link, customer responsibilities, IP, third-party services, disclaimers, 12-month liability cap, indemnification, temporary governing-law wording (§11), changes, contact.
- `/cancellation-policy`: tier table rendered from the single source of truth `CANCELLATION_POLICY_TABLE` (no drift between policy page and refund engine), worked refund examples, deposit basis, rescheduling/transfer fee, no-shows, how to cancel, planning calls, disputes, related documents.

## 5. Canonical/SEO/robots/sitemap integration

- All three pages set canonical metadata pointing at `https://teamtastic.events/{privacy,terms,cancellation-policy}` (browser-verified present).
- `sitemap.js` now includes `/privacy` (0.2/yearly), `/terms` (0.2/yearly), `/cancellation-policy` (0.4/monthly) with an honest `lastModified: 2026-08-29`; sitemap test extended to assert presence and honest dates. `robots.js` already allows all.
- Pre-existing follow-up re-affirmed: fabricated `TODAY` on older static sitemap entries is unchanged (tracked separately, §27-F).

## 6. Refund policy implementation alignment (verified; no redesign)

- The published policy is not aspirational — it matches the shipped machinery:
  - `computeCancellationPolicy` in `src/lib/cancellation-policy.js`: ≥7d→100%, 48h–7d→50%, <48h→25%, at/after/no-show→0%; `computeRefundAmountCents` rounds to the cent.
  - Office: `cancelHostedEvent`/`markEventNoShow` in `src/lib/server/office/deal-events.js` compute eligibility and amount over total `deal_payments` (deposits included) and route the actual issuance through a deliberate human action; Stripe webhook reconciles `refund.created/updated/failed` via `reconcile_stripe_refund`.
  - Self-service `/api/bookings/cancel|reschedule` cover only free planning calls (no funds involved).
- Conclusion: no redesign needed; the deposit-is-included wording on the policy page is accurate to `amountPaidCentsForDeal`.

## 7. Footer legal navigation and compliance cleanup

- Both footer variants now link `/privacy`, `/terms`, `/cancellation-policy` (new "Cancellations & Refunds" entry) and keep `mailto:hello@teamtastic.events`.
- Four placeholder social icons removed and `© 2024` → dynamic year in the experiences footer; dead `href="#"` eliminated from both footers.
- Regression guard added: `src/app/policy-pages.test.js` asserts the three links + mailto exist and no `href="#"` remains in `Footer.js`.

## 8. Lead-capture consent and deposit disclosures

- Five lead surfaces gained a consent line ("By submitting you agree to our privacy policy and terms. We may email you about your inquiry and occasionally share event ideas — you can opt out anytime.") linking `/privacy` + `/terms`: `CorporateLeadForm`, `TalkToMichaelModal`, `GameQuiz`, `HolidayChecklistForm`, `SoloDemo`.
- Deposit disclosures added where a purchase CTA sits next to a form: CorporateLeadForm (both full + success states), TalkToMichaelModal, GameQuiz success state.
- All captured by automated guards (policy-pages tests enumerate the five files).

## 9. FAQ consistency and stale-claim cleanup

- Found a factual inconsistency: the FAQ refund answer claimed "Self-service subscriptions can be cancelled anytime," but no subscription product exists in the repo → removed the false statement.
- Refund answer now references `https://teamtastic.events/cancellation-policy` for full details.
- FAQ tiers remain consistent with `CANCELLATION_POLICY_TABLE`.

## 10. Email transparency, unsolicited mail, and unsubscribe gap

- All user-facing contact is consistently `hello@teamtastic.events` (mailto in footers, error/reschedule copy, booking pages, all three policy pages).
- Gap (P3): outbound mail via `sendViaResend` carries no `List-Unsubscribe` header/link, and `suppression_list` is populated only by the Resend webhook (delivered/bounced/complained). Recommended follow-up: an office suppression UI plus an unsubscribe link on promotional sends; the privacy page currently directs opt-outs to email.

## 11. Legal identity and governing-law flags (owner input)

- No registered legal entity name, address, or jurisdiction exists anywhere in the repo; Terms use safe temporary wording ("governed by the laws of the jurisdiction in which Teamtastic operates") rather than inventing one.
- Cross-border wording in the privacy page does not assert a specific supervisory authority. These remain owner/legal decisions — see §27.

## 12. Trust claims and marketing statement audit

- Verified-accurate everywhere checked: deposit amounts ($200 corporate / $100 family), seat counts, minimums, pricing wording, "no credit card required," PO/invoicing support, cancellation tiers, "we'll reply within one business day."
- Unverified (flagged, not asserted): the blog's "1,200+ corporate game shows"; soft superlatives ("zero lag", "guaranteed high-energy experience") are generic marketing, not policy commitments.

## 13. Tracking, pixels, and consent infrastructure

- Pixels are consent-gated end to end: `AdPixels.js` initializes Meta Pixel and Google gtag (GA4 + Ads, Consent Mode v2 defaults) only after `effectiveConsent() === "granted"`; `ConsentBanner.js` drives the localStorage consent state and reloads on in-region grant/denial changes.
- PostHog runs through a first-party `/ingest` proxy with `respect_dnt: true`, `person_profiles: "identified_only"`, and `track()` filters PII fields. `lead_captured` is the only ad-conversion event. No session replay.
- Privacy page accurately reflects all of the above; no claims about email providers' DPA posture are made.

## 14. Data processors and cross-border transfers

- Privacy page lists the verified processor set actually present in code (Supabase, Stripe, Resend, Zoom, Google Calendar, Cloudflare Turnstile, PostHog, Meta, Google, Calendly-where-configured).
- Transfers wording is neutral (no unverifiable EU/UK adequacy assertions); Stripe card handling stated without PCI-scope claims.

## 15. Children / minors policy surface

- `/virtual-family-game-night` markets games with family participation. Privacy page states Teamtastic does not knowingly collect data from children under 13 and directs organizers to obtain parental consent; Terms require organizers to confirm participant eligibility. No data field anywhere collects a birthdate/age gate — accurate.

## 16. User-generated content policy surface

- Game formats include photo/meme rounds (e.g., Meme Battle, photo-proof themes). Privacy page gives Teamtastic a license to display submitted UGC for the event and lets organizers remove content; Terms mirror the ownership/responsibility split. No upload pipeline exists in the repo beyond quiz answers — disclosed honestly.

## 17. Recording and privacy-by-design

- No recording feature exists in the repo; privacy page states Teamtastic's software does not record events. Zoom/Meet host recording is controlled by the organizer (outside our software) — flagged as an owner/legal item to confirm for the hosted-event sales narrative.

## 18. Contact-point consistency

- Re-searched repo: every user-facing support address is `hello@teamtastic.events` (25 occurrences, all aligned). `michael@teamtastic.com` appears only in tests/office env config — internal only.

## 19. Deposit and pricing amount consistency

- Re-searched: $200 corporate / $100 family appear identically across `CorporateLeadForm`, `TalkToMichaelModal`, `GameQuiz`, `Hero`, `themes.js`, holiday/conversion/pricing pages, Terms, and cancellation-policy. No drift found.

## 20. Domain/brand consistency

- `src/app/resources/event-planning-guide` references `teamtastic.games` (a different public domain). Flagged for owner confirmation of ownership/intent; not changed.

## 21. Automated regression tests (11 new)

- `src/lib/cancellation-policy.test.js`: +4 tests pinning `CANCELLATION_POLICY_TABLE` to the refund engine at every threshold boundary.
- `src/app/sitemap.test.js`: +1 asserting the three policy routes with honest `2026-08-29` dates.
- `src/app/policy-pages.test.js` (new): policy pages render tolerant-to-JSX source checks (canonical, PolicyShell, `Last updated`, updated-date, placeholder-free); footer legal links + no `href="#"`; five lead forms each link privacy, terms, and opt-out.
- All pass in the suite (298 total).

## 22. Estate gates

- `npm test` → 298 passed (41 files). `npm run lint` → clean. `npm run typecheck` → clean. `npm run build` → clean; `/privacy`, `/terms`, `/cancellation-policy` prerendered as static `○`.

## 23. Responsive browser QC

- Playwright `policy-qc.js` against production build on `:3111`, 4 viewports (360/768/1280/1920): 16/16 PASS — status <500, canonical present, no horizontal overflow, zero `a[href='#']`, footer policy/mailto links present, required body copy found.

## 24. Dead-link and placeholder-anchor scan

- Remaining `href="#…"` are in-page scroll anchors with verified targets (`#availability`, `#get-quote`, `#holiday-availability`, `#holiday-quote`, `#theme-quote` all resolve to matching `id` sections). No dead anchors, no `© 202x` hardcode, no `[YOUR…`/TODO/lorem tokens in shipped source. The lone `#deposit-configuration-required` constant lives in dead `PAYMENT_CONFIG` (never rendered) — see §27-E.

## 25. Customer-journey walk

- Footer → policy pages → lead forms/quiz → Checkout (Stripe, deposit disclosure present) → booking (planning calls, honest fallback handling) → cancel/reschedule (self-serve for calls, office-mediated for paid events) → refund emails/copy: every step's claims are backed by the implemented systems verified above.

## 26. Final consistency re-search and change-scope summary

- Re-ran §18/§19/§24 sweeps after all edits; zero drift. All changes are additive or corrective; no production behavior, schema, or migration was modified. Prior seasonal-engine workstream untouched.

## 27. Findings classification (P0–P3)

**P0 (critical):** none.
**P1 (high):** none.
**P2 (medium — owner input, non-blocking):**
- A (LEGAL): entity name, registered address, governing law/jurisdiction not final — Terms use safe temporary wording; needs owner/legal sign-off before heavy commercial reliance.
- B (TRUST): blog claim "1,200+ corporate game shows" unverified in repo; confirm or soften.

**P3 (low / follow-ups):**
- C (EMAIL): no List-Unsubscribe / in-email opt-out; suppression_list is webhook-fed only → add office suppression UI + unsubscribe link.
- D (OWNER): confirm recording posture narrative for hosted events (Zoom/Meet accounts are organizer-controlled).
- E (DEAD CONFIG): `PAYMENT_CONFIG.depositUrl` fallback `#deposit-configuration-required` is never rendered at runtime (test-only reference) → remove or wire.
- F (PRE-EXISTING): fabricated `TODAY` `lastModified` on older sitemap static entries (existing follow-up #1; new policy entries already use honest dates).
- G (OWNER): confirm ownership/intent of `teamtastic.games` domain reference in event-planning-guide.
- H (UX): footer `isExperiencesPage` path list is stale (only two paths) → expand as experience pages ship.
- I (SOCIAL): placeholder socials removed; no live social profiles linked yet (add when accounts exist).

---

## Final verdict and sign-off

**Verdict: CLEAN BASELINE WITH NON-BLOCKING FOLLOW-UPS.**

The Teamtastic site now ships real, internally-consistent privacy, terms, and cancellation/refund policies that match the shipped payment and refund machinery: all footer dead links and stale copyright removed, every lead form carries consent and deposit disclosure, FAQ stale claims fixed, and everything is covered by new regression tests, clean lint/typecheck/build, and 16/16 responsive browser checks. No P0/P1 issues. The P2 items are documentation/legal inputs owned by the customer (entity/jurisdiction and the blog's provenance claim), not code defects, and none block launch; all P3 follow-ups are recorded above.